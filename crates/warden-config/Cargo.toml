[package]
name = "warden-config"
version = "0.1.0"
description = "Configuration model, loading, validation, and secrets"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
# `docs/architecture.md` section 3: this crate gets serde, toml, secrecy and core
# metadata. Not policy, not service, not a driver — the composition root maps this
# crate's output into those.
warden-core = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
secrecy = { workspace = true }
thiserror = { workspace = true }
