//! Reads a DSN from the source a connection names, and wraps it without ever
//! logging or returning its contents.
//!
//! Arrives in Task 2 (`docs/operations.md` section 3.3). Task 1 stops at parsing and
//! per-field validation, so this module is a placeholder until then.

#[cfg(test)]
mod tests {}
