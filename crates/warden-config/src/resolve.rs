//! Cross-field validation and the shape the composition root can actually build.
//!
//! Arrives in Task 2 (`docs/operations.md` section 3.2). Task 1 stops at parsing and
//! per-field validation, so this module is a placeholder until then.

#[cfg(test)]
mod tests {}
