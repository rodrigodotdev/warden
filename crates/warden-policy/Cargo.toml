[package]
name = "warden-policy"
version = "0.1.0"
description = "Policy engine, AllowDecision, and AuthorizedQuery"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
warden-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Shared fixtures (ADR-0049). Dev-only: `tests/architecture.rs` proves it appears in no
# normal dependency edge.
warden-testing = { workspace = true }
serde_json = { workspace = true }
